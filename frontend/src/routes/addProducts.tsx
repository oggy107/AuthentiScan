import { FC } from "react";

const AddProduct: FC = (): JSX.Element => {
    return (
        <div className="flex flex-grow">
            <div className="w-full h-ful">AddProduct</div>
        </div>
    );
};

export default AddProduct;
