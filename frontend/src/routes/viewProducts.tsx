import { FC } from "react";

const ViewProduct: FC = (): JSX.Element => {
    return (
        <div className="flex flex-grow">
            <div className="w-full h-ful">ViewProduct</div>
        </div>
    );
};

export default ViewProduct;
